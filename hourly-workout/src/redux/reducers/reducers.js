import { TOGGLE } from '../actions';

export function toggleReducer(state, action) {
    console.log(state.workout_selection);
}

export function rootReducer(state, action) {

    return state;
}

export default rootReducer;